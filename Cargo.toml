[package]
name = "go2rust-cli"
version = "0.1.0"
edition = "2021"
description = "A CLI that converts Go source files into Rust skeletons."
license = "MIT"

[dependencies]
anyhow = "1.0"
clap = { version = "4.5", features = ["derive"] }
regex = "1.10"
thiserror = "1.0"

